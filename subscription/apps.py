from django.apps import AppConfig


class SubscriptionConfig(AppConfig): # ignore this , it automatically generates wen u create app
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'subscription'

from django.apps import AppConfig


class FacewebConfig(AppConfig):  # this one creates automatically when app is created 
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'faceweb'
